<div class="container">
  <div class="row">
    <div class="col-8 offset-2">
      <div class="card mt-4">
        <div class="card-body">
          <h5 class="card-title">submission</h5>
          <p>
            <strong>ID</strong>: {{submission.id}}</p>
          <p>
            <p>
              <strong>Text</strong>: {{submission.text}}</p>
            <p>
              <strong>Description:</strong> {{submission.description}}</p>
            <a href="/">← Back To Home</a>
        </div>
      </div>
    </div>
  </div>
</div>